{
  "access_token": "${INSTAGRAM_ACCESS_TOKEN}",
  "user_id": "${INSTAGRAM_USER_ID}",
  "app_id": "${INSTAGRAM_APP_ID}",
  "app_secret": "${INSTAGRAM_APP_SECRET}",
  "note": "These values should be set via environment variables in Replit Secrets"
}
